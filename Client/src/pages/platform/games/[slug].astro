---
import Layout from "../../../layouts/Layout.astro";
import { getGameBySlug, getGameScreenshotsBySlug } from "../../../services/apiClient";
import {detail} from '../../../data'
import BannerDetailGame from "../../../components/game/BannerDetailGame.astro";
const {slug} = Astro.params;

const game = detail;
console.log(game)
---
<Layout title={game?.name} back={game?.background_image}>
    <nav class="px-5">
        <a href="/platform" class="flex gap-2 items-center text-zinc-400 hover:text-zinc-200 w-fit transition-all ease-linear">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="3" stroke="currentColor" class="size-4">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 15 3 9m0 0 6-6M3 9h12a6 6 0 0 1 0 12h-3" />
            </svg>
            <span>Volver</span>
        </a>
    </nav>
    <main class="p-5 flex flex-col gap-5">
        <BannerDetailGame 
        name={game?.name}
        background_image={game?.background_image}
        rating={game?.rating}/>
        <section class="flex gap-4">
            <article>
                <div class="p-5 rounded-xl bg-[#202024] border flex flex-col gap-2
                border-t-[#39393f] border-l-[#3d3d41] border-b-[#2d2d32] border-r-[#2d2d32]">
                    <h2 class="text-lg flex gap-2 items-center">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="3" stroke="currentColor" class="size-5">
                            <path stroke-linecap="round" stroke-linejoin="round" d="m11.25 11.25.041-.02a.75.75 0 0 1 1.063.852l-.708 2.836a.75.75 0 0 0 1.063.853l.041-.021M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Zm-9-3.75h.008v.008H12V8.25Z" />
                        </svg>
                        <span>Descripción</span>
                    </h2>
                    <p class="text-zinc-300 font-sans">{game?.description_raw}</p>
                </div>
            </article>
            <article>

            </article>
        </section>
    </main>
</Layout>